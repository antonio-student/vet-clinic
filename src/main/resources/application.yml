spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: vet-clinic
  mvc:
    throw-exception-if-no-handler-found: true
  docker:
    compose:
      lifecycle-management: ${SPRING_DOCKER_COMPOSE_LIFECYCLE:start-and-stop}


springdoc:
  swagger-ui:
    path: /swagger
